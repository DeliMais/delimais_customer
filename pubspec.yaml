name: delimais_customer
description: "Aplicação mobile voltada para os consumidores."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.6.6
  google_fonts: ^6.2.1
  text_scroll: ^0.2.0
  solar_icon_pack: ^0.2.1
  ionicons: ^0.2.2
  file_picker: ^8.1.4
  path_provider: ^2.1.5
  cross_file: ^0.3.4+2
  statusbarz: ^1.0.14

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  very_good_analysis: ^6.0.0
  flutter_launcher_icons: ^0.14.1

flutter:
  uses-material-design: true

  assets:
    - assets/
